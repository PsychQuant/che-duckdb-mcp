<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
    Intentionally empty: hardened runtime is the macOS 26 distribution trigger.
    che-duckdb-mcp reads local DuckDB files and downloads docs over HTTPS;
    it touches no TCC-gated privacy API, so no entitlement key is required for
    outside-MAS distribution. Add specific keys here only if hardened runtime
    breaks a specific API at runtime.
-->
<plist version="1.0">
<dict/>
</plist>
